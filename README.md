# unitinder

## Run the API

From this directory, use the project venv so FastAPI is available:

```bash
.venv/bin/uvicorn main:app --reload --port 8765
```

Or: `source .venv/bin/activate` then `uvicorn main:app --reload --port 8765`.
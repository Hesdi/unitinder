# Copy to .env and set your key: cp .env.example .env
# OPENAI_API_KEY is used to generate personalized teacher summaries per student.
OPENAI_API_KEY=

# Set to 1 to use the static JSON summary instead of AI (e.g. when testing without API).
UNITINDER_DISABLE_AI_SUMMARY=0

# Optional: for Azure OpenAI use the same endpoint/deployment as output.py, e.g.:
# OPENAI_BASE_URL=https://hesdi-mm4zauz8-eastus2.cognitiveservices.azure.com/openai/v1/
# OPENAI_MODEL=gpt-5.2-chat
# (If you get 401 "Incorrect API key", your key is likely Azure — set these and restart the API.)
